require('../node_modules/extpay')

    // self.importScripts('firebase/firebase-app.js', 'firebase/firebase-database.js');

    // alert("content.js");

    // const firebaseConfig = {
    //     databaseURL: process.env.REACT_APP_DATABASE,
    // };
    // var app = fireabse.initializeApp(firebaseConfig);
    // var database = firebase.getDatabase(app);

    // chrome.runtime.onMessage.addListener(
    //     function(request, sender, sendResponse) {
    //     console.log(sender.tab ?
    //                 "from a content script:" + sender.tab.url :
    //                 "from the extension");
    //     if (request.greeting === "hello")
    //         sendResponse({farewell: database});
    //     }
    // );

